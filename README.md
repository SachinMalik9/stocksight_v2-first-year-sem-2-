# stocksight_v2-first-year-sem-2-
Instead of a standard web app, I engineered this using a Dockerized microservices architecture. Independent Python robots scrape data in the background and feed it into an Elasticsearch cluster, allowing the UI to render live data in under 1 millisecond.
